lab4-1-2.py:
	可以改變 make_gfilter(variance, kernelsize)
	np.clip( H, a_min, a_max , out=H)

lab4-2.py:
	可以改變 make_gfilter(variance, kernelsize)
	wiener( egaufft, image, K value )

lab4-3.py:
	可以改變 make_gfilter(variance, kernelsize)
	wiener( egaufft, image, K value )